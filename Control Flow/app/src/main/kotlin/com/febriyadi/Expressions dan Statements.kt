package com.febriyadi

fun sum(value1: Int, value2: Int) = value1 + value2

fun main() {
    /*
    Expression Pada umumnya
    if (now > openOffice) {
        print("Office already open")
    } else {
        print("Office close")
    }
    val office = if (now > openOffice) "Office already open" else "Office close"
    print(office)
     */
    sum(1, 1 * 4)
}

